# Your own images, GIFs and videos
media/

# Your playlist and the editor's runtime files (each person makes their own)
config.js
config.backup.js
remote.js

# Leftovers from the old single-file version, kept locally only
_old_v1/

# Kraken Host app: dependencies and build output (npm install / dotnet publish recreate them)
app/node_modules/
app/sensors/bin/
app/sensors/obj/

# OS clutter
Thumbs.db
desktop.ini
.DS_Store
